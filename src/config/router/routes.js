const SERVER_URL = "http://localhost:3000";

const ADMIN_ROUTES = `${SERVER_URL}/admin`;
const BUS_ROUTES = `${SERVER_URL}/bus`;
const DRIVER_ROUTES = `${SERVER_URL}/driver`;
const PARENT_ROUTES = `${SERVER_URL}/parent`;
const SCHOOL_ROUTES = `${SERVER_URL}/school`;
const STUDENT_ROUTES = `${SERVER_URL}/student`;

export const ROUTES = {
  //admin routes
  ADMIN_LOGIN: `${ADMIN_ROUTES}/login`,

  //bus routes

  //driver routes

  //parent routes

  //school routes

  //student routes
};
